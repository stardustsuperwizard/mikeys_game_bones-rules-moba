# Implementation Plans

One plan per feature Issue, written by the planner session using
`.github/templates/implementation-plan.md` and named
`<issue-number>-<slug>.md`.

Plans are committed because implementation and review sessions start cold —
they see this directory, not the planner's reasoning. See
`docs/AGENT_WORKFLOW.md`.
