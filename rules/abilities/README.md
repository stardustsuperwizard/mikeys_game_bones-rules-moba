# rules/abilities/

Active ability definitions and execution logic. Each ability is a `MobaAbility` resource
that carries its own cast, targeting, and cost configuration. No numeric values are
hard-coded here — all data loads from `rules/data/abilities/`.
