# rules/data/

Balance source of truth for the MOBA ruleset.

Authored content lives here as `.tres` files (Godot resources) and is
loaded directly by GDScript. The `generated/` subdirectory is gitignored
and holds JSON exported for the Python balance harness only.
