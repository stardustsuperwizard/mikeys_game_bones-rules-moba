# rules/data/schema/

Hand-edited JSON configuration tables committed to version control: `state_transitions.json`
and `aim_assist.json`. Also holds the conformance suite fixtures used by the Python balance
harness to verify GDScript and Python formulas produce identical output.
