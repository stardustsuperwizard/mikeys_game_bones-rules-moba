# JSON Configuration Schema and Fixtures

Hand-authored JSON files defining game configuration: interrupt table, device aim assist multipliers, and conformance test fixtures.
These are committed to version control and read at runtime.
