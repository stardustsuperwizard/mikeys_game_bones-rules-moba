# rules/data/stat_blocks/

`.tres` resource files for reusable base-stat templates (e.g. "glass cannon", "tank").
Loadouts and enemy templates reference these to reduce data duplication. Authored in the
inspector; `MobaFormulas` reads values from the loaded resource.
