# rules/data/passives/

`.tres` resource files for passive traits and on-hit modifiers. Passives attach to
combatants via loadout and are evaluated by the effects subsystem each tick. Authored in
the Godot inspector; no passive logic lives in GDScript files outside `rules/effects/`.
