# rules/ai/

PvE AI controllers that produce `Intent` objects compatible with the input subsystem.
Each AI script implements a behaviour policy (e.g. melee aggressor, ranged skirmisher).
No scene nodes live here — only pure logic operating on combatant state.
