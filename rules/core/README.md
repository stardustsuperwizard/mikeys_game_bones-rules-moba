# rules/core/

Stat block definition, combat formulas, and damage resolution.

All code here is pure: functions take plain values and return plain values
with no `Node`, no scene tree, and no `get_tree()`.
