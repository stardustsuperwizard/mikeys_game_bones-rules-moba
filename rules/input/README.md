# rules/input/

Translates raw controller, keyboard, and touch input events into ruleset `Intent` objects.
Supports all three control schemes with full parity. No scene references — input is fed in
by the game layer, never polled from here.
