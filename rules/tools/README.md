# rules/tools/

Developer and balance tooling: data validators, stat-block importers, and helpers used by
the Python balance harness. Nothing here is required at runtime. Scripts may reference
`rules/data/generated/` because they produce or consume it.
