# rules/tools/

Headless authoring tools: data export and validation utilities.

Scripts here run with `--headless` to export `.tres` balance data to JSON
for the Python harness and to validate data files against their schemas.
