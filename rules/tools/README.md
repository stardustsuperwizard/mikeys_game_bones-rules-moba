# tools/

Headless authoring tools: data export scripts and validation utilities. Used to
export .tres resources to JSON for the Python balance harness and to verify data
integrity without launching the full editor.
