extends Node

# a demonstration comment that is deliberately far longer than the hundred character
# limit gdlint enforces a demonstration comment that is deliberately far longer than
# the hundred character limit gdlint enforces


func _demo() -> void:
	pass
